# intro-to-ros

```
mkdir -p catkin_ws/src
cd ~catkin_ws/
cd src/
catkin_init_workspace

git clone https://github.com/cmmakerclub/intro-to-ros.git ~
rsync -av ~/intro-to-ros/ catkin_ws/
cd ~/catkin_ws
catkin_make
```
